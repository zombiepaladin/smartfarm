<h1>Farms</h1>

<%= link_to 'New Farm', new_farm_path, class: "btn btn-info pull-right", role: "button" %>

<%= paginate @farms %>

<div id="farm-collection" class="row">
    <%= render @farms %>
</div>

<%= paginate @farms %>

